# 📚[6. Expressions — Python 3.12.4 documentation](https://docs.python.org/3/reference/expressions.html#conditional-expressions)
# syntax: 结果 = 值1 if 条件 else 值2
# 如果条件为真（true），则计算并返回表达式1的值；如果条件为假（false），则计算并返回表达式2的值。

# 🌰️比大小, 取最大值
a =1.2
b= 2.233
max_value = a if a > b else b
print(max_value)
